package com.knighttech.homelibrary.domain.model

import androidx.lifecycle.LiveData
import androidx.room.*

/*
@Dao
interface BookDao {
    @Insert
    suspend fun insert(book: Book) // Insert a new task.

    @Update
    suspend fun update(book: Book) // Update an existing task.

    @Delete
    suspend fun delete(book: Book) // Delete a task.

    //@Query("SELECT * FROM books WHERE isbn_13 = :isbn")
    //fun getBookByIsbn(isbn: String): Book? // Fetch a task by its ID.

    //@Query("SELECT * FROM books ORDER BY isbn_13 DESC")
    //fun getAllBooks(): List<Book> // Fetch all tasks.
}*/