<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/fondo_imagen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fondo_imagen"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeLibrary">
        <activity
            android:name=".ui.theme.AddBookFormActivity"
            android:exported="false"
            android:label="@string/title_activity_add_book_form"
            android:theme="@style/Theme.HomeLibrary" />

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="label" />

        <activity
            android:name=".BookListActivity"
            android:exported="false"
            android:label="@string/title_activity_book_list"
            android:theme="@style/Theme.HomeLibrary" />
        <activity
            android:name=".ScanScreenActivity"
            android:exported="false"
            android:label="@string/title_activity_scan_screen"
            android:theme="@style/Theme.HomeLibrary" />
        <activity
            android:name=".AddBookActivity"
            android:exported="false"
            android:label="@string/title_activity_add_book"
            android:theme="@style/Theme.HomeLibrary" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>